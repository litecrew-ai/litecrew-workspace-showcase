# Artifacts

Top level is split by business domain, matching `knowledge/`. Each business
directory holds live assets and archived Task output together; the business
README distinguishes them.

## Business directories

| Directory    | Purpose                                              | Manifest                |
| ------------ | ---------------------------------------------------- | ----------------------- |
| `writing/`   | Content products (blogs, editorial pipelines)        | `writing/README.md`     |

Live assets currently published:

- `writing/internet-archaeology-blog/` -- The Dead Web Gazette, a recurring
  dead-web memorial blog (pipeline + posts + static site). See its own
  `README.md` for architecture and run instructions.
